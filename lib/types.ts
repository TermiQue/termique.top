export type ProjectItem = {
  name: string;
  description: string;
  techStack: string[];
  github: string;
  demo: string;
  image: string;
  featured: boolean;
};

export type ToolItem = {
  name: string;
  description: string;
  url: string;
  category: string;
};